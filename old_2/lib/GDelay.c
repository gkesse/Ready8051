//===============================================
#include "GDelay.h"
//===============================================
#define G_MS 127
//===============================================
void GDelay_ms(uint ms) {
    uint i, j;
    for(i = 0; i < ms; i++) {
        for(j = 0; j < G_MS; j++);
    }
}
//===============================================
